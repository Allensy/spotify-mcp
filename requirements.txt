# Legacy requirements file for backward compatibility
# For new installations, use: pip install -e .
# Or from PyPI: pip install spotify-mcp
# See pyproject.toml for full dependencies

spotipy>=2.25.0
python-dotenv>=1.0.1
mcp>=1.1.0
